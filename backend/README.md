# Backend

This folder contains the FastAPI application, API routes, schema models, workflow orchestration, and demo-data services.

Environment setup will eventually use:

- `backend/.env.example` as the committed reference
- `backend/.env` for local real secrets
