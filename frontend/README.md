# Frontend

This folder contains the Next.js application for Customer Risk & Recovery Copilot.

Environment setup will eventually use:

- `frontend/.env.local.example` as the committed reference
- `frontend/.env.local` for local values
