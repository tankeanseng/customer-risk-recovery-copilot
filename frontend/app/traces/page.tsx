import { PlaceholderPage } from "../ui/placeholder-page";

export default function TracesPage() {
  return (
    <PlaceholderPage
      title="AI Workflow Trace"
      description="Technical operations console for workflow graphs, tool calls, structured outputs, and routing traces."
    />
  );
}

