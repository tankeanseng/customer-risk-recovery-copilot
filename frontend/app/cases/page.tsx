import { PlaceholderPage } from "../ui/placeholder-page";

export default function CasesPage() {
  return (
    <PlaceholderPage
      title="Cases"
      description="Workflow-oriented queue for recovery cases across review, approval, and resolution states."
    />
  );
}

